name: CI

# Manuell trigger för att testa pipelinen
on: workflow_dispatch

jobs:
  commit:
    uses: simonbrundin/deployment-pipeline/.github/workflows/commit-phase.yaml@main

  acceptance:
    needs: commit
    uses: simonbrundin/deployment-pipeline/.github/workflows/acceptance-phase.yaml@main
